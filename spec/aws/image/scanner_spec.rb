# frozen_string_literal: true

RSpec.describe Aws::Image::Scanner do
  it "has a version number" do
    expect(Aws::Image::Scanner::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
